# bad-basic

V1 intentionally degraded Retail model contract. It must be derived from `baseline-clean` only through the 13 declared anti-pattern mutations. The physical PBIP/TMDL model should remain openable, queryable, and scannable.
